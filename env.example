# ===================================================
# Gemini 图片生成器 - 环境配置模板
# ===================================================
# 使用方法：
#   1. 复制此文件并重命名为 .env
#   2. 填入您的 GCP 项目 ID
#   3. 将 GCP 服务账号密钥文件放在项目根目录，命名为 key.json
# ===================================================

# GCP 项目 ID（必填，如果 key.json 中已包含则可省略）
GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# GCP 区域（可选，默认为 global）
GOOGLE_CLOUD_LOCATION=global

# 服务账号密钥路径（可选，默认为项目根目录下的 key.json）
# GOOGLE_APPLICATION_CREDENTIALS=./key.json

